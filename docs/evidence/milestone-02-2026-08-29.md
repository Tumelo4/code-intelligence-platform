# Milestone 2 Verification Evidence — 2026-08-29

## Scope

Mandatory Codebase Review Skill registration, passive loading, exact revision pinning, required and optional reference loading, fail-closed validation, and PostgreSQL run provenance.

## Automated verification

Command:

```text
docker compose up --build --detach backend
```

The Java 21 Maven image compiled 29 production sources and 5 test sources. Result: 14 tests run, 0 failures, 0 errors, 0 skipped. This includes four architecture rules, domain validation, successful and failed orchestration, six filesystem/version/security adapter cases, and JDBC argument persistence.

## Runtime verification

`docker compose ps` reported backend, frontend, and PostgreSQL healthy. The backend actuator response was:

```json
{"status":"UP","groups":["liveness","readiness"]}
```

The initialized PostgreSQL `skill_run_provenance` table exposed all eight required columns: `run_id`, `skill_name`, `repository`, `branch`, `commit_sha`, `loaded_references`, `policy_version`, and `recorded_at`.

## Acceptance result

- AC-SKILL-001 passed: invalid source, missing required policy, wrong identity, revision mismatch, and symlink escape fail closed; orchestration does not persist failed loads.
- AC-SKILL-002 passed: a valid checkout loads mandatory and selected optional references and records the exact pinned version.
- AC-SKILL-003 passed: all architecture dependency rules remain green.
