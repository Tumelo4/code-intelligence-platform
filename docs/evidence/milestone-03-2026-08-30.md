# Milestone 3 Verification Evidence — 2026-08-30

## Scope

Credential-isolated repository authorization and access validation for GitHub App, public HTTPS Git, staged ZIP uploads, and opt-in local development paths; connection persistence and selection APIs.

## Automated verification

Command:

```text
docker compose up --build --detach
```

The Maven Eclipse Temurin 21 image compiled and packaged the backend. Result: 30 tests run, 0 failures, 0 errors, 0 skipped. Coverage includes architecture boundaries, all four source contracts, fail-closed validation, local-mode policy, GitHub App JWT and installation-token exchange, controlled public-Git egress, staged ZIP digest validation, application orchestration, JDBC persistence, and presentation mapping.

## Runtime verification

Backend, frontend, and PostgreSQL reported healthy. The actuator endpoint returned `UP` for liveness and readiness.

The live POST endpoint validated `https://github.com/octocat/Hello-World.git` through Git smart-HTTP discovery and returned HTTP 201 with an opaque repository ID. PostgreSQL stored only:

```text
PUBLIC_GIT_URL | https://github.com/octocat/Hello-World.git
```

A URL containing embedded credentials returned HTTP 400. The GET selection endpoint returned the persisted credential-free connection after the backend was rebuilt and restarted.

The `repository_connection` schema contains `repository_id`, `source_type`, `safe_locator`, and `validated_at` only.

## Acceptance result

- AC-REPO-001 passed: every MVP source has an immutable domain contract.
- AC-REPO-002 passed: invalid or inaccessible sources fail before persistence or acquisition.
- AC-REPO-003 passed: GitHub authorization credentials remain inside the infrastructure probe; domain, API, and database values contain no secret fields.
- AC-REPO-004 passed: all architecture dependency tests remain green.
