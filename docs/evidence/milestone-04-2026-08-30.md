# Milestone 4 Verification Evidence — 2026-08-30

## Scope

Checkout-free exact-revision Git acquisition, bounded ZIP extraction, bounded local snapshots, immutable-original and writable-working trees, scoped GitHub App credentials, credential-free source persistence, acquisition status persistence, and durable Compose storage.

## Automated verification

Command:

```text
docker compose up --build --detach
```

The backend compiled and packaged under Eclipse Temurin 21. Result: 39 tests run, 0 failures, 0 errors, 0 skipped. Coverage includes the architecture boundary; unsafe ZIP paths and expansion limits; direct JGit blob materialization; symlink rejection; local `.git` exclusion; archive and local deterministic SHA-256 revisions; application dispatch; credential-free source reconstruction; and JDBC acquisition persistence.

## Runtime verification

Backend, frontend, and PostgreSQL reported healthy. A live public connection for `https://github.com/octocat/Hello-World.git` was acquired at `HEAD`. The API returned exact commit `7fd1a60b01f91b314f59955a4e4d4e80d8edf11d` with revision kind `GIT_COMMIT` and zero skipped submodules.

PostgreSQL persisted the exact revision, requested revision, skipped-submodule count, timestamp, and internal tree locations. The public response contained no credential or filesystem fields. Filesystem inspection found no `.git` or temporary bare-repository directory. Permissions were:

```text
dr-xr-xr-x original
-r--r--r-- original/README
drwxr-xr-x working
-rw-r--r-- working/README
```

After rebuilding and restarting the backend, the GET acquisition endpoint returned the same exact commit and both stored trees remained readable through named Compose volumes.

## Acceptance result

- AC-ACQUIRE-001 passed: a requested Git revision resolves to and persists an exact commit SHA.
- AC-ACQUIRE-002 passed: network and credential leases are lexical to bare object transfer and close before materialization.
- AC-ACQUIRE-003 passed: blobs are materialized without checkout, hooks, submodule recursion, or LFS execution.
- AC-ACQUIRE-004 passed: unsafe paths, links, duplicates, file counts, per-file sizes, and expanded sizes fail closed.
- AC-ACQUIRE-005 passed: original and working trees are separate, metadata-free, correctly permissioned, and durable across restart.
- AC-SAFE-004 passed: acquisition executes no repository-controlled command.
