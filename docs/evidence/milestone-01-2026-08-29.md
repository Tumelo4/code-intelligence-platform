# Milestone 1 Verification — 2026-08-29

Environment: macOS host, Docker Desktop 29.6.1, Docker Compose 5.3.0. Backend verification ran in Java 21.0.12; frontend ran with Node 24 locally and Node 22 in its image.

| Check | Deterministic command | Result |
|---|---|---|
| Backend and architecture | `docker build --progress=plain -t code-intelligence-backend:latest backend` | PASS — 5 tests, 0 failures (4 architecture, 1 domain) |
| Frontend lint | `npm run lint` | PASS — 0 warnings/errors |
| Frontend build | `npm run build` | PASS — production and TypeScript checks completed |
| Dependency audit | `npm audit --audit-level=high` | PASS — 0 vulnerabilities |
| Stack startup | `docker compose up --build --detach` | PASS |
| Runtime health | `docker compose ps` | PASS — all three services healthy |
| Backend endpoint | `GET http://localhost:8080/actuator/health` | PASS — `status: UP` |
| Frontend endpoint | `GET http://localhost:3000` | PASS — HTTP 200 |

The host Maven executable used Java 26, which ArchUnit 1.4.1 cannot fully inspect. That non-authoritative run was discarded; the recorded backend result is from the required Java 21 image. An initial annotation-driven ArchUnit form reported zero discoverable tests, so the rules were converted to explicit JUnit tests. A deliberate empty-layer failure was then resolved with package markers, after which all four rules executed and passed.
