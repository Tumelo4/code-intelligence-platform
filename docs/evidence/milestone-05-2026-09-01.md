# Milestone 5 Verification Evidence — 2026-09-01

## Scope

Passive, bounded repository inventory for Java/Maven and mixed repositories: stack detection, normalized repository-relative path categories, nested and multi-module Maven metadata, exact acquisition-revision linkage, persistence, and a filesystem-path-free API.

## Automated verification

Command:

```text
docker compose up --build --detach
```

The backend compiled and packaged under Eclipse Temurin 21. Result: 48 tests run, 0 failures, 0 errors, 0 skipped. Inventory coverage includes mixed-root and multi-module Maven discovery; Java and build-system detection; conventional and declared roots; dependencies, plugins, plugin executions, Surefire, and Failsafe; scripts and repository path categories; DTD and external-entity rejection; module escape rejection; symlink and configured-limit rejection; application orchestration; JSONB persistence; API mapping; and architecture boundaries.

## Runtime verification

Backend, frontend, and PostgreSQL reported healthy. The public repository `https://github.com/Tumelo4/code-intelligence-platform.git` was acquired at exact commit `5b5e38ee22f8557c686e7a8fbd0746c80d83910a` and inventoried without invoking Maven or repository commands.

The live API detected Java and TypeScript, Maven, `backend/src/main/java`, `backend/src/test/java`, the backend POM, frontend package manifest and lockfile, three Docker/Compose files, documentation, eight Maven dependencies, the Spring Boot Maven plugin, Java 21, and 146 inspected files. The response contained only normalized relative paths and no acquisition filesystem location.

After backend restart, GET returned the same exact revision and persisted report from PostgreSQL.

## Acceptance result

- AC-INVENTORY-001 passed: Java/Maven and mixed-repository stack evidence is detected passively.
- AC-INVENTORY-002 passed: roots are normalized, deduplicated, and restricted beneath the acquisition tree.
- AC-INVENTORY-003 passed: Maven coordinates, modules, Java version, dependencies, plugins, executions, Surefire, and Failsafe are deterministic.
- AC-INVENTORY-004 passed: scripts and all specified repository path categories are recorded as relative paths.
- AC-INVENTORY-005 passed: malicious XML, symlinks, escapes, and limit violations fail closed.
- AC-INVENTORY-006 passed: revision-linked JSONB inventory survives restart and the API exposes no internal path.
