# Milestone 7 Verification Evidence — 2026-09-04

## Scope

Deterministic, exact-revision Git intelligence over an acquired read-only bare object database: reachable commit traversal, rename-aware churn, pseudonymous ownership, bounded change coupling, PostgreSQL persistence, and a filesystem-path-free and personally identifying information-free API.

## Automated verification

Command:

```text
docker compose up --build --detach
```

The backend compiled and packaged under Eclipse Temurin 21.0.12. Result: 66 tests run, 0 failures, 0 errors, 0 skipped. Git-intelligence coverage includes repeatable ordering, exact-revision traversal, add/modify/delete and rename churn, pseudonymous author normalization, coupling support and strength, traversal truncation, changed-file and diff-byte bounds, unsafe history symlinks, missing revisions, non-Git acquisitions, persistence mapping, API mapping, and architecture boundaries.

## Runtime verification

Backend, frontend, and PostgreSQL reported healthy. The live workflow connected to the public repository, acquired exact Git commit `b008c3bfdf2602e40810a130e34ccd6f9f02ba14`, and produced 26 reachable commits, 222 file histories, and 129 coupling pairs with `historyTruncated=false`.

The serialized response contained neither the internal acquisition root nor email-address markers. After restarting the backend container, GET returned the same revision and the same 26 commit, 222 file-history, and 129 coupling-pair counts from PostgreSQL.

## Acceptance result

- AC-GIT-001 passed: repeated analysis of the same revision produced equal ordered results.
- AC-GIT-002 passed: fixture histories cover add, modify, delete, rename, and exact reachable membership with normalized churn.
- AC-GIT-003 passed: normalized equivalent identities share a stable 64-character lowercase SHA-256 pseudonym; raw author contact data is absent.
- AC-GIT-004 passed: coupling support, normalized strength, deterministic ordering, and configured pair bounds are enforced.
- AC-GIT-005 passed: missing/non-Git revisions, unsafe history links, changed-file and diff-byte limits fail closed; commit limits report explicit truncation.
- AC-GIT-006 passed: exact-revision results survived backend restart and API output exposed no internal acquisition path or email address.
